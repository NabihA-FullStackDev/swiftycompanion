# Swifty Companion by naali

## installation

A la racine du projet:
```
npm install
```

Toujours à la racine, créer un fichier `.env` en se basant sur le fichier `.env-template`  
Les informations a remplacer sont sur `votre-intra => settings => API`
